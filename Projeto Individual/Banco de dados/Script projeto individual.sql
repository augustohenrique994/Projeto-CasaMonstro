create database Projeto;
use projeto;
create table Cadastro (
EmailCadastro varchar(65),
NomeUsuario varchar(45),
SenhaCadastro varchar(45),
ConfirmarSenha varchar(45)
);
create table login (
NomeUsuario varchar(45),
Senhausuario varchar(45),
emailUsuario varchar(65));